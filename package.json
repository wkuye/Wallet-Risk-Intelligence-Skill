{
  "name": "pharos-wallet-analyzer-skill",
  "version": "1.0.0",
  "description": "Pharos Agent Center Skill — analyzes wallet reputation, activity, and risk signals using the Pharos public RPC.",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --require ts-node/register src/index.ts",
    "build": "tsc",
    "analyze": "node --require ts-node/register src/index.ts"
  },
  "keywords": ["pharos", "wallet", "analyzer", "skill", "agent-center", "onchain", "reputation"],
  "license": "MIT-0",
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}